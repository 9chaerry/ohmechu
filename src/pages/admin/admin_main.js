import { createHeader } from '/src/default_layout/admin_header.js';
import { createFooter } from '/src/default_layout/footer.js';
document.addEventListener('DOMContentLoaded', () => {
  createHeader();
  createFooter();
});
